apiVersion: v1
kind: Service
metadata:
  name: sre-frontend-service
  labels:
    app: sre-frontend
    component: frontend
spec:
  selector:
    app: sre-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: NodePort
